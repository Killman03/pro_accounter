-- Миграция: Удаление поля payment_date из таблицы coffee_machines
-- Дата: 2024-01-XX
-- Причина: Поле больше не используется, так как дата следующего платежа 
--          теперь рассчитывается динамически от последнего платежа

-- Удаляем колонку payment_date из таблицы coffee_machines
ALTER TABLE coffee_machines DROP COLUMN IF EXISTS payment_date;

-- Комментарий: Поле payment_date больше не нужно, так как:
-- 1. Напоминания теперь рассчитываются от последнего платежа клиента
-- 2. Дата следующего платежа = последний_платеж + 30 дней
-- 3. Это более гибкая и точная система 